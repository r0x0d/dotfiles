# dotfiles

My personal dotfiles stuff.

## Getting started

This set of dotfiles uses [dotbot](https://github.com/anishathalye/dotbot) to
manage the configs.

You can use a make command if you want, or install standalone configs, like this:

```bash
# To install configs related to work laptop
make work

# Or, to install standalone configs
./install-standalone bash
```
