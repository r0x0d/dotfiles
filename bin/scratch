#!/usr/bin/env bash

TEMPORARY_DIRECTORY=$(mktemp -d)
TEMPORARY_FILENAME="${1:-scratch.py}"
TEMPORARY_FULL_PATH="$TEMPORARY_DIRECTORY/$TEMPORARY_FILENAME"

touch "$TEMPORARY_FULL_PATH"

$EDITOR $TEMPORARY_FULL_PATH
echo $TEMPORARY_FULL_PATH
